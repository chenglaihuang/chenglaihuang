---
description: SSO default
alwaysApply: true
---

# SSO default

If you see any AWS permission errors, you can run `duo sso duolingo-main/okta-function-eng-940168671796` to fix it. If problem still exists, run the command together with the AWS commands together in one command.
